# Aniwordle, built site (deploy artifact)

This repo contains ONLY the compiled output of the Aniwordle Astro project
(`dist/`). Hostinger Git auto-deploy serves these files from `public_html`.
Do not edit by hand; it is regenerated by `npm run deploy:git` in the source repo.
